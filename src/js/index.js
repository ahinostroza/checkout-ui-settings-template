import prueba from './components/hola.js'

(function (Pragma, windows, events) {
    const functions = {
        initialize: () => {
            const orderForm = vtexjs.checkout.orderForm
            orderForm
            prueba()
        },
        ready: () => {

        }
    }
    functions.initialize()
})()